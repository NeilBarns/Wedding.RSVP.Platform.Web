import { CalendarDays, Clock, ExternalLink, MapPin, Shirt } from 'lucide-react'
import { formatWeddingDate } from '../../../lib/utils/formatDate'
import type { PublicWedding, WeddingEvent } from '../types'

const labels = { ceremony: 'Ceremony', reception: 'Reception', other: 'Other' } as const
function formatTime(value: string) { const [hours, minutes] = value.split(':').map(Number); if (!Number.isInteger(hours) || !Number.isInteger(minutes)) return value; return new Intl.DateTimeFormat(undefined, { hour: 'numeric', minute: '2-digit' }).format(new Date(2000, 0, 1, hours, minutes)) }

export function EventDetailsSection({ wedding, events }: { wedding: Pick<PublicWedding, 'weddingDate'>; events: WeddingEvent[] }) {
  return <section id="details" className="scroll-mt-24 bg-[var(--color-surface)] px-5 py-20 sm:px-8 sm:py-28"><div className="mx-auto max-w-5xl"><div className="text-center"><p className="text-xs font-semibold uppercase tracking-[0.25em] text-[var(--color-muted)]">The celebration</p><h2 className="mt-3 font-[var(--font-display)] text-4xl sm:text-5xl">Wedding details</h2></div>{events.length ? <div className="mt-12 grid gap-5 md:grid-cols-2">{events.map((event) => <article key={event.id} className="rounded-[var(--radius-lg)] border border-[var(--color-border)] bg-[var(--color-background)] p-7"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[var(--color-accent)]">{labels[event.eventType]}</p><h3 className="mt-3 font-[var(--font-display)] text-3xl">{event.title}</h3><dl className="mt-6 space-y-4 text-[var(--color-muted)]"><div className="flex gap-3"><CalendarDays className="mt-0.5 size-5 shrink-0" aria-hidden="true" /><div><dt className="sr-only">Date</dt><dd>{formatWeddingDate(event.eventDate)}</dd></div></div>{event.startTime ? <div className="flex gap-3"><Clock className="mt-0.5 size-5 shrink-0" aria-hidden="true" /><div><dt className="sr-only">Time</dt><dd>{formatTime(event.startTime)}{event.endTime ? ` – ${formatTime(event.endTime)}` : ''}</dd></div></div> : null}{event.venueName || event.addressLine ? <div className="flex gap-3"><MapPin className="mt-0.5 size-5 shrink-0" aria-hidden="true" /><div><dt className="sr-only">Location</dt><dd>{event.venueName ? <strong className="block font-semibold text-[var(--color-text)]">{event.venueName}</strong> : null}{event.addressLine ? <span className="whitespace-pre-line">{event.addressLine}</span> : null}</dd></div></div> : null}{event.dressCodeOverride ? <div className="flex gap-3"><Shirt className="mt-0.5 size-5 shrink-0" aria-hidden="true" /><div><dt className="font-semibold text-[var(--color-text)]">Dress code</dt><dd className="whitespace-pre-line">{event.dressCodeOverride}</dd></div></div> : null}</dl>{event.description ? <p className="mt-6 whitespace-pre-line leading-7 text-[var(--color-muted)]">{event.description}</p> : null}{event.mapUrl ? <a href={event.mapUrl} target="_blank" rel="noopener noreferrer" className="mt-6 inline-flex min-h-11 items-center gap-2 rounded-full border border-[var(--color-border)] px-5 font-semibold">View location <ExternalLink className="size-4" aria-hidden="true" /></a> : null}</article>)}</div> : <div className="mt-12 grid gap-5 sm:grid-cols-2"><article className="rounded-[var(--radius-lg)] border border-[var(--color-border)] bg-[var(--color-background)] p-7"><CalendarDays className="size-6 text-[var(--color-accent)]" aria-hidden="true" /><h3 className="mt-5 font-[var(--font-display)] text-2xl">Save the date</h3><p className="mt-2 text-[var(--color-muted)]">{formatWeddingDate(wedding.weddingDate)}</p></article><article className="rounded-[var(--radius-lg)] border border-[var(--color-border)] bg-[var(--color-background)] p-7"><MapPin className="size-6 text-[var(--color-accent)]" aria-hidden="true" /><h3 className="mt-5 font-[var(--font-display)] text-2xl">Ceremony &amp; reception</h3><p className="mt-2 text-[var(--color-muted)]">Venue and timing details will be shared as they are finalized.</p></article></div>}</div></section>
}
