import { motion, useReducedMotion } from 'framer-motion'
import { ArrowDown, CalendarDays } from 'lucide-react'
import { formatWeddingDate } from '../../../lib/utils/formatDate'
import type { PublicWeddingDetails, WeddingHeroContent } from '../types'
import { weddingCountdown } from '../utils'
import { ContentImage } from './ContentImage'

export function WeddingHero({ wedding, content }: { wedding: PublicWeddingDetails; content: WeddingHeroContent | null }) {
  const reduceMotion = useReducedMotion()
  const countdown = weddingCountdown(wedding.weddingDate)
  const names = `${wedding.partnerOneName} & ${wedding.partnerTwoName}`
  return <section id="top" className="relative isolate flex min-h-[78vh] scroll-mt-24 items-center overflow-hidden px-5 py-16 sm:px-8 sm:py-20"><div className="absolute inset-0 -z-10 bg-[radial-gradient(circle_at_20%_20%,color-mix(in_srgb,var(--color-secondary)_18%,transparent),transparent_35%),radial-gradient(circle_at_80%_70%,color-mix(in_srgb,var(--color-accent)_14%,transparent),transparent_38%)]" aria-hidden="true" /><motion.div initial={reduceMotion ? false : { opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: reduceMotion ? 0 : 0.55 }} className={`mx-auto grid w-full max-w-6xl items-center gap-10 ${content?.mediaUrl ? 'lg:grid-cols-[1.05fr_0.95fr]' : ''}`}><div className={content?.mediaUrl ? 'text-left' : 'text-center'}><p className="text-xs font-semibold uppercase tracking-[0.3em] text-[var(--color-muted)]">{content?.eyebrow || "We're getting married"}</p>{content?.headline ? <><h1 className="mt-6 font-[var(--font-display)] text-5xl leading-[0.98] text-[var(--color-primary)] sm:text-7xl">{content.headline}</h1><p className="mt-5 font-[var(--font-display)] text-2xl text-[var(--color-accent)]">{names}</p></> : <h1 className="mt-6 font-[var(--font-display)] text-5xl leading-[0.95] text-[var(--color-primary)] sm:text-7xl lg:text-8xl">{wedding.partnerOneName}<span className="mx-3 block text-[0.55em] italic text-[var(--color-accent)] sm:inline" aria-hidden="true">&amp;</span>{wedding.partnerTwoName}</h1>}{content?.subheadline ? <p className="mt-6 max-w-2xl whitespace-pre-line text-lg leading-8 text-[var(--color-muted)]">{content.subheadline}</p> : null}<p className={`mt-8 inline-flex items-center gap-2 text-base tracking-wide sm:text-lg ${content?.mediaUrl ? '' : 'justify-center'}`}><CalendarDays className="size-5" aria-hidden="true" />{formatWeddingDate(wedding.weddingDate)}</p><div className="mt-7" aria-label={countdown.value === null ? countdown.label : `${countdown.value} ${countdown.label}`}>{countdown.value === null || countdown.value === 0 ? <p className="font-[var(--font-display)] text-2xl">{countdown.label}</p> : <><p className="font-[var(--font-display)] text-5xl tabular-nums">{countdown.value}</p><p className="mt-1 text-sm uppercase tracking-[0.18em] text-[var(--color-muted)]">{countdown.label}</p></>}</div><div className={`mt-9 flex flex-wrap gap-3 ${content?.mediaUrl ? '' : 'justify-center'}`}><a href="#rsvp" className="inline-flex min-h-12 items-center rounded-full bg-[var(--color-primary)] px-7 font-semibold text-white">RSVP</a><a href="#details" className="inline-flex min-h-12 items-center gap-2 rounded-full border border-[var(--color-border)] bg-[var(--color-surface)] px-7 font-semibold">Wedding details <ArrowDown className="size-4" aria-hidden="true" /></a></div></div>{content?.mediaUrl ? <div className="overflow-hidden rounded-[var(--radius-lg)] border border-[var(--color-border)] bg-[var(--color-surface)] shadow-[var(--shadow-soft)]"><ContentImage src={content.mediaUrl} alt={content.mediaAltText} eager className="aspect-[4/5] w-full object-cover" /></div> : null}</motion.div></section>
}
