import { Heart } from 'lucide-react'
import { formatWeddingDate } from '../../../lib/utils/formatDate'
import type { WeddingStoryEntry } from '../types'
import { ContentImage } from './ContentImage'

export function StorySection({ entries }: { entries: WeddingStoryEntry[] }) {
  return <section id="story" className="scroll-mt-24 px-5 py-20 sm:px-8 sm:py-28"><div className="mx-auto max-w-5xl"><div className="text-center"><Heart className="mx-auto size-6 text-[var(--color-accent)]" strokeWidth={1.5} aria-hidden="true" /><p className="mt-5 text-xs font-semibold uppercase tracking-[0.25em] text-[var(--color-muted)]">Our story</p><h2 className="mt-3 font-[var(--font-display)] text-4xl sm:text-5xl">The moments that brought us here</h2></div><div className="mt-14 space-y-12">{entries.map((entry, index) => <article key={entry.id} className={`grid items-center gap-8 md:grid-cols-2 ${index % 2 ? 'md:[&>*:first-child]:order-2' : ''}`}>{entry.imageUrl ? <ContentImage src={entry.imageUrl} alt={entry.imageAltText} className="aspect-[4/3] w-full rounded-[var(--radius-lg)] border border-[var(--color-border)] object-cover" /> : <div className="hidden aspect-[4/3] rounded-[var(--radius-lg)] bg-[var(--color-surface)] md:block" aria-hidden="true" />}<div>{entry.eventDate ? <p className="text-sm font-semibold uppercase tracking-[0.15em] text-[var(--color-accent)]">{formatWeddingDate(entry.eventDate)}</p> : null}{entry.title ? <h3 className="mt-3 font-[var(--font-display)] text-3xl">{entry.title}</h3> : null}<p className="mt-4 whitespace-pre-line leading-8 text-[var(--color-muted)]">{entry.body}</p></div></article>)}</div></div></section>
}
