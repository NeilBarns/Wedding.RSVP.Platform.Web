export function PublicationBadge({ published }: { published: boolean }) { return <span className="inline-flex items-center gap-1.5 rounded-full border border-[var(--color-border)] bg-[var(--color-background)] px-2.5 py-1 text-xs font-semibold"><span className={`size-2 rounded-full ${published?'bg-[var(--color-success)]':'bg-[var(--color-muted)]'}`} aria-hidden="true" />{published?'Published':'Draft'}</span> }
export function PublicationField({ checked, onChange, disabled }: { checked: boolean; onChange: (value: boolean) => void; disabled?: boolean }) { return <label className="flex min-h-14 cursor-pointer items-center justify-between gap-4 rounded-[var(--radius-md)] border border-[var(--color-border)] bg-[var(--color-background)] p-4"><span><span className="block font-semibold">Published</span><span className="block text-sm text-[var(--color-muted)]">Only published content appears on the public wedding site.</span></span><input type="checkbox" checked={checked} disabled={disabled} onChange={(event)=>onChange(event.target.checked)} className="size-5 accent-[var(--color-primary)]" /></label> }
