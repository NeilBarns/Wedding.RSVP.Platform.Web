import { ImageOff } from 'lucide-react'; import { useState } from 'react'
export function MediaPreview({ url, alt }: { url: string; alt: string }) { const [brokenUrl,setBrokenUrl]=useState<string|null>(null); const broken=brokenUrl===url; if(!url||broken)return <div className="flex aspect-video items-center justify-center rounded-[var(--radius-md)] border border-dashed border-[var(--color-border)] bg-[var(--color-background)] text-sm text-[var(--color-muted)]"><ImageOff className="mr-2 size-5" aria-hidden="true" />{broken?'Image preview unavailable':'No media URL provided'}</div>; return <img src={url} alt={alt||'Content preview'} onError={()=>setBrokenUrl(url)} className="aspect-video w-full rounded-[var(--radius-md)] border border-[var(--color-border)] object-cover" /> }
